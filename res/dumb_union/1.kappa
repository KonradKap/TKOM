union() Kappa {
    case(3) {
        int a;
    };
};

union() Keepo {
    default {
        int a;
    };
};

union() PogChamp {
    default {
        int a;
    };
};

Kappa(3) kappa;
Keepo(kappa.a) keepo;
PogChamp(keepo.a);
