struct(decider) Kappa {
    union() Keepo {
        case(1) {
            int[2];
        };
        case(2) {
            int[3];
        };
        default {
            int;
        };
    };
    Keepo(decider);
};

Kappa(3);
Kappa.Keepo(1);
