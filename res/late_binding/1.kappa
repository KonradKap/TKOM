struct Kappa {
    int decider;
    union(decider) Keepo {
        case(1) {
            int[2];
        }
        case(2) {
            int[3];
        }
        default {
            int;
        }
    };
};

Kappa;
